const {
    CsvFileLoader,
} = require('../src/librairies');

const csv_file_loader = CsvFileLoader.getInstance();
const seed_path = './test/seeds/feed_rss_result';


describe('CSV Import functionnal test', () => {
    it('Should be able to import csv file', (done) => {
        done();
    });
});
