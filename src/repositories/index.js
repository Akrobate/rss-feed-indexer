'use strict';

const {
    MongoDbRepository,
} = require('./MongoDbRepository');
const {
    RssFeedItemRepository,
} = require('./RssFeedItemRepository');

module.exports = {
    MongoDbRepository,
    RssFeedItemRepository,
};
